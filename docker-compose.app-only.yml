version: '3.8'

services:
  app:
    build: .
    image: diario-de-classe-backend:prod
    environment:
      - NODE_ENV=production
      # Configure o DATABASE_URL conforme seu ambiente externo
    ports:
      - '3000:3000'
    restart: unless-stopped
